import React, { Component } from 'react';
import ReactRedirect from 'react-fetch';
class Submit extends Component {  
   render() {
    window.location.reload();
    return(
        <h>Submit page</h>
    );
  } 

}
export default Submit;